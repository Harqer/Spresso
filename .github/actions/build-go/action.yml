name: 'Build Go Gateway'
description: 'Compiles the Go Gateway production binary'
runs:
  using: "composite"
  steps:
    - name: Build Gateway
      shell: bash
      working-directory: aura-gateway-go
      run: |
        mkdir -p ../dist
        go build -o ../dist/aura-gateway ./cmd/server/main.go
